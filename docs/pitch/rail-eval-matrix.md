# Rail Evaluation Matrix

| Rail | Implemented capabilities | Sandbox evidence | Webhook posture | Failure simulation | Go-live notes |
| --- | --- | --- | --- | --- | --- |
| Stripe | PaymentIntent top-ups, signature-verified webhooks, recent PaymentIntent reconciliation, optional Connect onboarding skeleton | Sandbox acceptance materially proven for supported wallet top-up flow: PaymentIntent created, signed webhook processed, provider state fetched, internal wallet settlement applied | `Stripe-Signature` HMAC verification, raw `partner_event` persistence, event ID dedupe | PaymentIntent failed/canceled states only; no `FAILED_LATE` claim | Live keys rejected by showcase config; payouts require recipient Connect account mapping before support can be claimed |
| TrueLayer | Hosted payment wallet top-ups, request signing, signature-verified webhooks/status processing, business-account payouts | Top-up sandbox acceptance materially proven: 3000 minor-unit top-up moved to pending at initiation, then webhook/status settlement moved 3000 from pending to available. Business-account payout sandbox acceptance materially proven: 1500 minor-unit withdrawal returned `processing`, then settlement moved 1500 out of pending | `Tl-Signature` verification, raw `partner_event` persistence, event ID dedupe, fetched payment/payout status processing | Payment and payout failed states only; no `FAILED_LATE` claim | Live credentials rejected by showcase config; AIS account verification, `external_account` payouts, `payment_source` payouts, and production readiness remain unclaimed |
